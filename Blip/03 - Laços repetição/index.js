// Usando For e o While, são usados para repetição (loop).
// Vai repetir até uma condição for verdadeira.

let tabela = [0, 2, 4, 6, 8, 10]


for (let numeros = 0; numeros < tabela.length; numeros++) {
    const element = tabela[numeros];
    console.log(element)
}

